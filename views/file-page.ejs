<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>File Uploader</title>
  <link rel="stylesheet" href="/styles.css">
  <script src="/modal.js" defer></script>
</head>
<body>
<%= console.trace(currentUser.fullname) %>
<%- include("partials/add-folder-modal.ejs") %>
<%- include("partials/add-file-modal.ejs") %>
<% if (currentUser) {%>
    <div class="top-bar">
            <h1><%= currentUser.fullname %>'s file uploader</h1>
            <a href="/log-out">Log out</a>
    </div>
    <div class="file-option">
        <div class="folder-overall">
            <div class="folders-list">
                <% folders.forEach((folder) => { %>
                    <!-- now when you click it, it has a param you can use to look up info -->
                    <a href="/folder/<%= folder.id %>" class="folder-btn"><%= folder.name %></a>
                <% }) %>
            </div>
            <div class="folder-contents">
                <h2>contents here</h2>
            </div>
        </div>
        <div class="folder-page-options">
            <div class="new-file-folder">
                <button class="new-folder-btn">New folder</button>
                <!-- move inside the folder -->
                <button class="new-file-btn">New file</button>
            </div>
        </div>         
    </div>
<% } %>
  
</body>
</html>